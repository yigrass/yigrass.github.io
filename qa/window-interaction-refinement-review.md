# Window Interaction Refinement review

Project: yigrass.github.io · Task path: Personal Website / QQ Space MVP / Window Interaction Refinement · Active task: Window Interaction Refinement · Status: completed · Position: main

Date: 2026-09-20. Parent: QQ Space MVP. Return point: QQ Space MVP / Review updated desktop. Local baseline: `28faed9`. Implementation and available checks are complete; actual browser acceptance remains at the parent task.

## Scope

三个窗口共用四边和四角的指针缩放行为。拖动从当前计算尺寸开始，限制在桌面内，并以 380×240 CSS 像素作为常规最小尺寸；可用空间不足时以桌面边界为先。左边和上边缩放保持对边位置，坐标对齐整数像素。尺寸仅存在于当前窗口 DOM 中，关闭重建后恢复原有默认大小；最小化及最大化还原保留本次尺寸，视口缩小后会把窗口限制到可用桌面内。

最大化时隐藏缩放边界；640px 及以下视口保持原有铺满布局。指针结束、取消、丢失捕获，或窗口最小化、最大化、关闭以及浏览器调整大小时，释放缩放状态。开启自定义光标后，窗口边缘和缩放中的指针仍使用对应的方向提示。修正了原有矮屏强制高度规则，使手动缩放可以生效。

资源管理器工具栏只保留“向上”，删除后退、前进、“文件夹”按钮及历史记录逻辑。左侧目录面板常驻，树内节点仍可展开和折叠。地址选择、驱动器入口、空目录提示与对象计数继续使用当前目录状态。

按用户要求，网站配色没有修改。四个黑灰候选仅显示在对话预览中：A 墨黑 `#202020`、B 炭灰 `#383838`、C 暖石墨 `#4A4844`、D 中性灰 `#606060`。预览同时展示标题栏和选中条，并可点击目录查看选中效果。预览源文件位于本次任务的对话目录 `C:\Users\a\.codex\visualizations\2026\09\19\01a0ba4a-dba9-77f0-a01e-3956931bce16\desktop-gray-candidates.html`，不被网站引用，也不构成网站依赖。

## Validation

更新后的 `assets/app.js` 通过 V8 语法编译。直接执行该文件中的纯尺寸计算函数，检查八个方向、尺寸下限、桌面边界、对边锚点及矮屏情况；11 个指定预期结果检查、200 组整数偏移检查均通过。进一步检查发现半像素输入可能使对边移动 1 像素，已先取整左/上坐标再计算宽高；修正后含半像素偏移的 200 组检查通过。这些是几何计算检查，不是浏览器指针事件测试。

当前 CSS 与本地 HEAD 的颜色字面值序列一致，确认未把候选颜色应用到网站。已检查源代码不再含后退/前进/文件夹工具按钮、目录历史索引或隐藏左栏逻辑。窗口创建和关闭路径经源码检查，未新增尺寸持久化或改动设置保存格式。

独立配色预览已读回：包含四个候选和五个唯一 ID，脚本编译通过；动态查询对应元素均存在，目录按钮更新选中样式、地址和位置标题。可选 Tweak 控件仅修改预览内的颜色。该预览用于配色判断，不是实际网页的浏览器截图。

本轮保留此前浏览器 URL 策略限制，未改用其他入口绕过。实际边框命中、指针捕获、窗口还原以及不同屏幕下的排版仍待浏览器确认。建议在三个窗口分别拖动边和角，检查最小尺寸与桌面边界，再执行最大化/还原和关闭/重新打开；同时确认资源管理器只有“向上”且左栏始终可见。

本次仅修改本地实现、README 与任务记录，没有 Git 写操作或远端通信。此前已完成的报告及像素素材版本保持原样。
