# Reader Menu and Navigation

Project: yigrass.github.io. Task path: Personal Website / Reader Menu and Navigation. Active task: Reader Menu and Navigation. Status: completed. Position: main. Date: 2026-09-25. Return point: Personal Website / Review reader menu and navigation.

本轮移除了正文边缘翻章控件及其悬停、淡出样式。标题栏下新增“文件”“查看”、短竖线和两个 60×28 像素的箭头翻章按钮；两份菜单目前仅包含禁用的“功能开发中”。文件与查看互斥展开，支持外部点击、焦点离开、Escape 和 Tab 关闭；菜单打开时左右键切换菜单，不翻章。关闭菜单后保留前台阅读器的左右键跨卷翻章，边界按钮可见但禁用。

目录开关改成独立 30 像素窄栏里的竖向页签，高 78 像素；三行字面字符分别为 `<` 或 `>`。常规窗口的实际测量中，目录右边界与按钮左边界均为 573，按钮右边界约 602.11，正文左边界为 603，因此没有跨越分隔线或覆盖正文。收起目录后同样保持按钮位于正文左边。窄屏默认收起目录。

使用现有本地预览完成浏览器验证：默认窄视口可显示完整菜单与按钮；1440×900 的临时视口下检查了普通窗口和最大化窗口。文件菜单中的占位项呈灰色；左右键从文件切换到查看时，chapter-2 地址和正文不变；Escape 收起菜单并将焦点返回查看按钮。鼠标翻到第三章，再用右键跨卷到第四章，地址正确更新。长章节滚到底后，最后一行底边距阅读区顶端约 30.24 像素，正文行高 30.4 像素，保留了末行可滚至顶部的行为。临时视口已复原，预览保留在第二章开头。

`check-final.log` 记录完整检查通过：32 项测试、231 项桌面回归断言、源码语法、成品校验、17 个构建页面、发布内容字节和任务控制检查。新交互回归覆盖菜单互斥、方向键不误翻章、关闭与焦点恢复、占位项禁用、章节边界、目录字符方向及窗口关闭时事件释放。`check.log` 保留首次检查结果：阅读器测试全部通过，一项已有构建测试仍固定引用已删除的海边壁纸；现改为读取 siteConfig.wallpaper 并继续检查源文件和 dist 的字节一致性，未改变实际壁纸。

书签、用户显示偏好及可自定义快捷键记为 Reader Bookmarks and Preferences 待办；既有 Novel Reader Customization 仍保留为每书内部视觉配置任务。未改小说成品合同、收到的小说文本、图标、壁纸或路由规则。本轮由 Main 直接实现，未委派；未执行 Git 写操作或远端操作。
